<div class="row">
  <div class="col-md-8">
    <% if @student.preferiti.any? %>
      <h3>Microposts ( @user.microposts.count )</h3>
      <ol class="microposts">
        <%= render @preferiti %>
      </ol>
      <%= will_paginate @preferiti %>
    <% end %>
  </div>
</div>
